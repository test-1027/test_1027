name: path-filter

on:
    push:
        paths:
            - 'my-app/**'

jobs:
    path-filter:
        runs-on: ubuntu-latest
        steps:
            - name: echo hello
              run: echo "hello"